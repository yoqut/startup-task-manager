# Message handlers are defined in commands.py, tasks.py, reports.py, and admin.py.
# This file is intentionally left minimal — it exists only to preserve the legacy import
# in older __init__.py revisions.
